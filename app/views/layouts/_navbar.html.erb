<nav class="navbar navbar-expand-lg border rounded bg-secondary">
  <div class="container-fluid">
    <a class="navbar-brand text-white" href=<%= root_path %> ><b>ToDo Bot</b></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <% if @current_user %>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link active text-white" aria-current="page" href=<%= root_path %> >Home</a>
          </li>
        </ul>
      </div>
    <% end %>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        <% if @current_user %>
          <li class="nav-item">
            <a class="nav-link active text-capitalize text-white" href="#"><b>User: <%= @current_user.first_name+" "+@current_user.last_name%></b></a>
          </li>
          <li class="nav-item">
            <a <button class="btn btn-outline-danger text-white border border-white" href= <%= end_session_account_block_logins_path %> > SignOut</button> </a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>